name: droplet_server
description: Serveur Droplet pour Railway — signaling WebRTC + TURN
version: 1.0.0

environment:
  sdk: ^3.10.3

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_web_socket: ^1.0.4
  args: ^2.4.2
  uuid: ^4.5.1

dev_dependencies:
  test: ^1.24.0
  http: ^1.1.0
